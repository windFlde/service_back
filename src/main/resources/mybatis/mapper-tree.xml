<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.mapper.TreeMapper">

    <!-- List<Tree> queryTree(@Param("pid")String id,@Param("user")Users user);-->
    <!--<select id="queryTree" resultType="Tree">

         SELECT
    DISTINCT t.*
    FROM
    t_user_role tur,

    t_role_permission trp,

    t_tree t
    WHERE

    tur.roleid = trp.roleid
    AND trp.permissionid = t.id
    AND tur.userid =#{user.id}
    and t.pid= #{pid}
    and state = 0
    </select>-->

</mapper>